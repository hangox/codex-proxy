appId: com.codex-proxy.app
productName: Codex Proxy

directories:
  output: release

files:
  - dist-electron/**/*
  - config/**/*
  - public/**/*
  - public-desktop/**/*
  - electron/assets/**/*
  - package.json
  # No node_modules or dist/ needed — backend is bundled into dist-electron/server.mjs

# Static assets unpacked from asar (filesystem access at runtime)
asarUnpack:
  - "config/**/*"
  - "public/**/*"
  - "public-desktop/**/*"
  - "**/*.node"

# curl-impersonate binaries as extra resources (outside asar)
extraResources:
  - from: bin/
    to: bin/
    filter:
      - "**/*"

win:
  target:
    - target: nsis
      arch: [x64]
  icon: electron/assets/icon.png
  signAndEditExecutable: false

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  perMachine: false

mac:
  target:
    - target: dmg
      arch: [arm64, x64]
  icon: electron/assets/icon.png
  category: public.app-category.developer-tools
  # No Apple Developer certificate — electron-builder auto-falls back to
  # ad-hoc signing (codesign -s -). Users see "unidentified developer"
  # warning and can right-click → Open to bypass.

linux:
  target:
    - target: AppImage
      arch: [x64]
  icon: electron/assets/icon.png
  category: Development
